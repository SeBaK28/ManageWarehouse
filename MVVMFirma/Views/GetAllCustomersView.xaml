<local:WszystkieViewBase x:Class="MVVMFirma.Views.GetAllCustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" Name="CustomersGrid" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding CustomerID}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Adres" Binding="{Binding Address}"/>
                <DataGridTextColumn Header="Osoba kontaktowa" Binding="{Binding ContactPerson}"/>
                <DataGridTextColumn Header="Numer telefonu" Binding="{Binding Phone}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Podatek" Binding="{Binding TaxID}"/>
                <DataGridTextColumn Header="Utworzono" Binding="{Binding CreatedAt}"/>
                <DataGridTextColumn Header="Edytowano" Binding="{Binding UpdatedAt}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</local:WszystkieViewBase>

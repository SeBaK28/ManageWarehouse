<local:WszystkieViewBase x:Class="MVVMFirma.Views.GetAllReceiptsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" Name="GoodsReceiptsGrid" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding GoodsReceiptID}"/>
                <DataGridTextColumn Header="Zamówienie zakupu Id" Binding="{Binding PurchaseOrderID}"/>
                <DataGridTextColumn Header="Magazyn Id" Binding="{Binding WarehouseID}"/>
                <DataGridTextColumn Header="Data przyjęcia" Binding="{Binding ReceiptDate}"/>
                <DataGridTextColumn Header="Utworzono" Binding="{Binding CreatedAt}"/>
                <DataGridTextColumn Header="Edytowano" Binding="{Binding UpdatedAt}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</local:WszystkieViewBase>

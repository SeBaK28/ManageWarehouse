<local:WszystkieViewBase x:Class="MVVMFirma.Views.GetAllInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Imie" Binding="{Binding  Name}"/>
                <DataGridTextColumn Header="Adres" Binding="{Binding  Address}"/>
                <DataGridTextColumn Header="Osoba kontaktowa" Binding="{Binding  ContactPerson}"/>
                <DataGridTextColumn Header="Numer telefonu" Binding="{Binding  Phone}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding  Email}"/>
                <DataGridTextColumn Header="Data Zamowienia" Binding="{Binding  OrderDate}"/>
                <DataGridTextColumn Header="Kwota Calkowita" Binding="{Binding  TotalAmount}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>

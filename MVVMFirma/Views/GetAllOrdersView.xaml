<local:WszystkieViewBase x:Class="MVVMFirma.Views.GetAllOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" Name="PurchaseOrdersGrid" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding PurchaseOrderID}"/>
                <DataGridTextColumn Header="Dostawca Id" Binding="{Binding SupplierID}"/>
                <DataGridTextColumn Header="Data zamówienia" Binding="{Binding OrderDate}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Łączna kwota" Binding="{Binding TotalAmount}"/>
                <DataGridTextColumn Header="Utworzono" Binding="{Binding CreatedAt}"/>
                <DataGridTextColumn Header="Edytowano" Binding="{Binding UpdatedAt}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</local:WszystkieViewBase>

<UserControl x:Class="MVVMFirma.Views.StocktakingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <Button Content="Wczytaj Excel" Command="{Binding LoadExcelCommand}" Width="120" Margin="0,0,0,10"/>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" Height=" 250" IsReadOnly="True" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Nazwa}"/>
                <DataGridTextColumn Header="Wartosc" Binding="{Binding Wartosc}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
